plugin_config:
  enable_cpu: true    # 启用 CPU 监控 (使用率、核心数、温度)
  enable_mem: true    # 启用内存监控
  enable_net: true    # 启用流量监控 (若 Android/Proot 报错请关闭)
  enable_disk: true   # 启用磁盘监控 (若 FileNotFoundError 报错请关闭)